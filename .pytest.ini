[pytest]
norecursedirs = .git tetris
console_output_style = progress
testpaths = pytests
addopts = --disable-pytest-warnings -s --tb=long --color=yes
filterwarnings =
    ignore::DeprecationWarning